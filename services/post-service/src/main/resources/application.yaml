spring:
  application:
    name: post-service
  config:
    import: "configserver:"
  cloud:
    config:
      uri: http://localhost:8888
gamification:
  rabbitmq:
    exchange: gamification.exchange
    routing-key: gamification.event.post.created